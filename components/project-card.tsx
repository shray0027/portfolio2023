import { TProjectCardProps } from '../types/props.types';

const ProjectCard: React.FC<TProjectCardProps> = () => {
  return <div id="project-card-container" className="rounded"></div>;
};

export default ProjectCard;
